# GTD Todo Application - Environment Configuration

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================

# PostgreSQL user (used for database access)
POSTGRES_USER=postgres

# PostgreSQL password (should be changed in production)
POSTGRES_PASSWORD=postgres

# Database name
POSTGRES_DB=todo_app

# ============================================================================
# Backend (ASP.NET Core) Configuration
# ============================================================================

# Execution environment: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Development

# Database connection string
# Format: Server=<host>;Port=<port>;Database=<dbname>;User Id=<user>;Password=<password>;
BACKEND_CONNECTION_STRING=Server=postgres;Port=5432;Database=todo_app;User Id=postgres;Password=postgres;

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# ============================================================================
# JWT Authentication Configuration
# ============================================================================

# Secret key for signing JWT tokens (min 32 characters, must be changed in production)
# Generate with: openssl rand -base64 32
JWT_SECRET_KEY=your-secret-key-min-32-chars-change-in-prod

# JWT token issuer
JWT_ISSUER=TodoApp

# JWT token audience
JWT_AUDIENCE=TodoApp.Client

# JWT token expiration in minutes (default: 1440 = 24 hours)
JWT_EXPIRATION_MINUTES=1440

# ============================================================================
# Frontend (Next.js) Configuration
# ============================================================================

# Backend API URL (used by frontend to call backend)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Application name for UI branding
NEXT_PUBLIC_APP_NAME=GTD Todo

# ============================================================================
# Docker Compose Configuration
# ============================================================================

# Set to empty/unset for production builds
# Development mode enables hot-reload and debug features
COMPOSE_PROJECT_NAME=todo-app

# ============================================================================
# Production Recommendations
# ============================================================================

# For production deployment:
# 1. Change POSTGRES_PASSWORD to a strong password
# 2. Generate a new JWT_SECRET_KEY: openssl rand -base64 32
# 3. Update CORS_ALLOWED_ORIGINS to your frontend domain
# 4. Set ASPNETCORE_ENVIRONMENT to Production
# 5. Use HTTPS and secure database connections
# 6. Store secrets in a secure secrets manager (not .env file)
